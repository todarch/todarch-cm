spring:
  application:
    name: cm
  cloud:
    config:
      uri: http://localhost:7001
      fail-fast: true
      label: feature/todarch-cm
  mail:
    host: smtp.gmail.com
    port: 587
    username: todarch.app@gmail.com
    password: "${SPRING_MAIL_PASSWORD}"
    properties:
      mail.smtp:
        auth: true
        starttls.enable: true
